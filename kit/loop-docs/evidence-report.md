<!-- TEMPLATE -->
# Implementation Evidence Report

<!-- Generated by /loop-evidence when the loop reaches a success candidate.
     This is what the human reviews INSTEAD of the full diff. -->

## 1. Requirements addressed

<!-- REQ-xxx: how it was satisfied -->

## 2. Changed files

## 3. Verification executed

<!-- Baseline from .loop/baseline-verify.log ("n/a" when absent); Result from
     .loop/last-verify.log. A red→green row proves the gate discriminates. -->

| Command | Baseline | Result |
|---|---|---|

<!-- When .loop/docs/acceptance-checklist.md is filled in, mirror its final
     state here. For `run` rows name the observation artifact
     (.loop/observations/...) that proves the behavior was actually seen —
     each row's SINGLE literal path, byte-for-byte as the checklist cites it.
     Every .loop/observations/ literal in this report is validated against
     the checklist and the evaluator manifest; mention superseded captures
     prefix-less (iter2-AC-xxx-probe.log), never in full path form. -->

| AC | REQ | Expectation | Method | Status | Evidence |
|---|---|---|---|---|---|

## 4. Starting unknowns & assumptions made

<!-- The unknowns the run STARTED with (from .loop/docs/unknowns.md: questions
     asked, deferred defaults, direction verdicts), then every AS-N entry from
     .loop/docs/assumptions.md with its gate adjudication. Or "None". -->

## 5. Spec diff

| Area | Changed? | Notes |
|---|---|---|
| Product requirement | no | |
| API contract | no | |
| Data model | no | |
| UX behavior | no | |
| Security boundary | no | |

## 6. Risks

## 7. Points needing human judgment

## 8. Lessons for future runs

<!-- 3-7 bullets a FUTURE run should reuse: non-obvious design decisions and
     why, approaches tried and rejected, repository traps discovered (build
     quirks, flaky checks, hidden couplings). This section is read from
     .loop/docs/run-archive/ by later contract/plan sessions. Or "None". -->
