<!-- TEMPLATE -->
# Progress Log

<!--
MUTABLE. This file + git history are the loop's ONLY memory across iterations
(each iteration starts with a fresh context). Every iteration appends an entry.
Record failed attempts explicitly so the next iteration does not repeat them.
-->

<!-- Entry format:
## Iteration N — <date>
- did: ...
- verify: pass/fail (which checks)
- failed attempts (do not retry): ...
- next: the single next step
-->
